## コミットメッセージ

- 英語で書く
- `update:` / `fix:` / `feat:` / `refactor:` / `docs:` / `chore:` などのプレフィックスで始める
- 件名は命令形・小文字始まり・ピリオドなし

例:

```
update: migrate to Vite+ toolchain
fix: prevent null reference in facility selector
feat: add monthly P/L screen
```
