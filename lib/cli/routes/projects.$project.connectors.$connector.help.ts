export const help = `leuco projects <p> connectors <c> / operations on a connector

usage / leuco projects <p> connectors <c> [subcommand]

subcommands:
  remove / remove this connector
  rename <new> / rename
  start / enable
  stop / disable
  restart / restart this connector
  set-tokens / update Slack tokens (run with -h)
  download-file / download a private Slack file
  schedules / manage cron entries (run with -h)`
