/** Consumer-owned command guidance for embedded Leuco runtimes. */
export type LeucoHostInstructions = {
  header: string
  slackCommands: string
  scheduleCommands: string
  localCommandHygiene: string
}
